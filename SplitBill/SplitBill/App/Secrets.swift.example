// Secrets.swift.example
// Copy this file to Secrets.swift and fill in your real keys.
// Secrets.swift is gitignored — this template file is safe to commit.

enum Secrets {
    static let geminiAPIKey = "YOUR_GEMINI_API_KEY_HERE"
}
